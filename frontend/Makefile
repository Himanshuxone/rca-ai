# Makefile for React Frontend (CRA)

install:
	npm install

start:
	npm start

build:
	npm run build

lint:
	npx eslint src --ext .js,.jsx

format:
	npx prettier --write src

test:
	npm test

clean:
	rm -rf node_modules build

reset: clean
	rm -rf package-lock.json
